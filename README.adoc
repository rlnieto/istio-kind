== Contenido
* *create-cluster:* crea un cluster kind, instala istio y etiqueta el ns *default* para que istio inyecte los sidecars
* *pod-utils-deployment.yaml:* despliega un pod con utilidades (openssl y curl) en el namespace *utils*
* *dockerfiles:* contiene los dockerfiles para los pods desplegados en el cluster
